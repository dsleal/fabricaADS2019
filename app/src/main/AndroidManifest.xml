<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="contrato.com">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.administrador.EditTipoPrestador"></activity>
        <activity android:name=".activities.administrador.AddTipoPrestador" />
        <activity
            android:name=".activities.administrador.TTipoPrestador"
            android:label="@string/title_activity_ttipo_prestador"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.prestador.CadastroPrestador" />
        <activity android:name=".activities.cliente.CadastroCliente" />
        <activity android:name=".activities.Cadastro" />
        <activity android:name=".activities.Login" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.administrador.Administrador" />
    </application>

</manifest>